@extends('pages.dashboard.layouts.index')

@section('table-content')
    <div class="flex flex-col gap-5">
        <!-- Toolbar/Header -->
        <div class="flex flex-col md:flex-row md:items-center md:justify-between gap-4">
            <div>
                <h1 class="text-xl font-bold">إدارة البلدان</h1>
                <div class="text-sm text-gray-500">
                    عرض {{ $countries->firstItem() ?? 1 }}-{{ $countries->lastItem() ?? $countries->count() }} من أصل {{ $countries->total() ?? $countries->count() }} بلد
                </div>
            </div>
            <div class="flex gap-2">
                <a href="{{ route('countries.create') }}" class="px-4 py-2 bg-blue-500 text-white rounded hover:bg-blue-600">
                    <i class="ki-filled ki-plus text-sm"></i>
                    إضافة بلد
                </a>
                {{-- أزرار إضافية مثل استيراد CSV أو فلاتر يمكن إضافتها هنا --}}
            </div>
        </div>

        <!-- الجدول -->
        <div class="kt-card kt-card-grid min-w-full">
            <livewire:dashboard.country-table :countries="$countries" />
        </div>
    </div>
@endsection
